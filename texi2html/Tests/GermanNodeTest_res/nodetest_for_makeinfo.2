** `&~"#)'@{([-|`_\^@@]=+@}*$%!:;.?/<>§_@pounds{}_@questiondown{}_@exclamdown{}_@enddots{}_@dots{}' doesn't appear in menus
** `@code{node}' doesn't appear in menus
** `@asis{node}' doesn't appear in menus
** `node' doesn't appear in menus
** `@^{E}' doesn't appear in menus
*** Node equivalent with `@aa{}' allready used `@ringaccent{a}' 
*** Node equivalent with `@asis{node}' allready used `node' 
*** Node equivalent with `@code{node}' allready used `node' 
*** Node equivalent with `@^{E}' allready used `@^E' 
*** Node equivalent with `i' allready used `@dotless{i}' 
** @ubaraccent only valid with `bdhklnrtzBHKLNRTZ', not `a' 
** @H only valid with `uUoO', not `a' 
** @udotaccent only valid with `bdehiklmnorstuvwyzBDEHIKLMNORSTUVWYZ', not `a' 
** @udotaccent only valid with `bdehiklmnorstuvwyzBDEHIKLMNORSTUVWYZ', not `a' 
** @ubaraccent only valid with `bdhklnrtzBHKLNRTZ', not `a' 
** @H only valid with `uUoO', not `a' 
** @H only valid with `uUoO', not `a' 
** @H only valid with `uUoO', not `a' 
** @udotaccent only valid with `bdehiklmnorstuvwyzBDEHIKLMNORSTUVWYZ', not `a' 
** @udotaccent only valid with `bdehiklmnorstuvwyzBDEHIKLMNORSTUVWYZ', not `a' 
** @ubaraccent only valid with `bdhklnrtzBHKLNRTZ', not `a' 
** @ubaraccent only valid with `bdhklnrtzBHKLNRTZ', not `a' 
