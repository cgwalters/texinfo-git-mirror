*** Node equivalent with `@aa{}' already used `@ringaccent{a}' 
*** Node equivalent with `@asis{node}' already used `node' 
*** Node equivalent with `@code{node}' already used `node' 
*** Node equivalent with `@^{E}' already used `@^E' 
** `&~"#)'@{([-|`_\^@@]=+@}*$%!:;.?/<>§_@pounds{}_@questiondown{}_@exclamdown{}_@enddots{}_@dots{}' doesn't appear in menus
** `@code{node}' doesn't appear in menus
** `@asis{node}' doesn't appear in menus
** `node' doesn't appear in menus
