** @H only valid with `uUoO', not `a' 
** @dotaccent only valid with `eiEIcgzCGZ', not `a' 
** @v only valid with `cszCSZdlnrtDLNRTeE', not `a' 
** @v only valid with `cszCSZdlnrtDLNRTeE', not `a' 
** @H only valid with `uUoO', not `a' 
** @dotaccent only valid with `eiEIcgzCGZ', not `a' 
** @H only valid with `uUoO', not `a' 
** @H only valid with `uUoO', not `a' 
** @v only valid with `cszCSZdlnrtDLNRTeE', not `a' 
** @v only valid with `cszCSZdlnrtDLNRTeE', not `a' 
** @dotaccent only valid with `eiEIcgzCGZ', not `a' 
** @dotaccent only valid with `eiEIcgzCGZ', not `a' 
